---
layout: folder-index.njk
---
<h1>Code snippets</h1>

{% for category in collections.snippetCategories %}
    <h2>{{ category }}</h2>

    <ul>
        {% for item in collections.snippets %}
            {% if "/snippets/" + category in item.url %}
                <li><a href="{{ item.url }}">{{ item.data.title or item.fileSlug }}</a></li>
            {% endif %}
        {% endfor %}
    </ul>
{% endfor %}
