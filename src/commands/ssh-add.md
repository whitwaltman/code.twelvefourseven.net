using two or more github accounts on the same machine and having ssh credentials not match

```zsh
git rv # alias for git remote -v
ssh-add ~/.ssh/whitwaltman
```

i just name my file the name of my github username