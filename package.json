{
  "name": "code.twelvefourseven.net",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "git@github.alt:whitwaltman/code.twelvefourseven.net.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npx eleventy",
    "build:clean": "npm run clean && npx eleventy",
    "build:all": "NODE_ENV=production npm run build:clean && npm run format",
    "clean": "rm -rf .site",
    "postbuild": "npx pagefind --site .site --glob \"**/*.html\"",
    "debug": "DEBUG=* npx eleventy",
    "format": "node _utils/format.js",
    "start": "npx eleventy --serve --incremental"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "flat-cache": "^6.1.22",
    "shiki": "^4.0.2"
  },
  "overrides": {
    "minimatch": "^10.2.1"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "linkedom": "^0.18.12",
    "pagefind": "^1.4.0",
    "prettier": "^3.7.4"
  }
}
