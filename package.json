{
  "name": "code.twelvefourseven.net",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "git@github.alt:whitwaltman/code.twelvefourseven.net.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npx eleventy",
    "build:clean": "rm -rf _site && npx eleventy",
    "build:all": "npm run build && npm run format",
    "debug": "DEBUG=* npx eleventy",
    "format": "node utils/format.js",
    "start": "npx eleventy --serve --incremental",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "overrides": {
    "minimatch": "^10.2.1"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "linkedom": "^0.18.12"
  }
}
