---
title: Backwards slicing
tags:
- ruby
- javascript
- arrays
---

Some languages allow you to use a negative index value (e.g. Ruby, JavaScript). A negative index is an offset that traverses backwards from the end of the array.

```ruby
arr = (?A..?E).to_a
```