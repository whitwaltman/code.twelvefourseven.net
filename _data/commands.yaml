- command: git clone --depth 1 <repository_url>
  title: shallow git clone
  description: repository copy with truncated commit history
  dettails: --depth 1 fetches only the single most recent commit
