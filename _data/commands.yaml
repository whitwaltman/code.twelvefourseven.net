- command: git clone --depth 1 <repository_url>
  title: shallow git clone
  description: repository copy with truncated commit history
  details: --depth 1 fetches only the single most recent commit
  tags:
    - git
- command: git branch -m <new_name>
  title: rename a git branch
  description:
  details:
  tags:
    - git